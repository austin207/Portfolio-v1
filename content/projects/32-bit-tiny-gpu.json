{
  "title": "32-Bit Tiny GPU",
  "description": "A 32-bit SIMT GPU designed solo from ISA to silicon signoff: a 28-instruction ISA, SystemVerilog RTL verified by 391 cocotb tests, the axelcc C-subset compiler, and a signoff-clean GDSII on SkyWater 130 nm through OpenLane.",
  "image": "/project-images/32-bit-tiny-gpu/GPU.png",
  "tags": [
    "SystemVerilog",
    "SIMT",
    "GPU Architecture",
    "ISA Design",
    "OpenLane",
    "Sky130",
    "Physical Design",
    "cocotb",
    "Compilers",
    "C",
    "Python"
  ],
  "status": "RTL, verification and GDSII signoff complete — compiler and kernels ongoing",
  "duration": "March 2026 - Present",
  "category": "VLSI & Computer Architecture",
  "github": "https://github.com/austin207/32-bit-Tiny-GPU",
  "overview": "32-Bit Tiny GPU is a ground-up implementation of a programmable SIMT GPU — from the instruction set and SystemVerilog RTL, through functional verification, all the way to a signoff-clean GDSII layout and a working compiler. Every layer was designed solo.\n\nThe architecture is built on a custom 28-instruction 32-bit ISA with a multi-stage pipeline scheduler, warp dispatch across parallel cores, and a memory controller arbitrating between instruction and data memory. Verification is a cocotb harness of 391 tests spanning 15 RTL test suites. On top of the hardware sits axelcc, a C-subset compiler with a kernel-parameter ABI, which compiles kernels that run and are verified on the simulated RTL — including transformer multi-head attention and a depthwise-separable CNN pipeline.",
  "objectives": [
    "Design a 28-instruction 32-bit SIMT ISA covering arithmetic, logic, memory, control flow and the signed operations ML workloads need",
    "Implement the full RTL datapath in SystemVerilog — dispatcher, scheduler, fetcher, decoder, ALU, LSU, register file, program counter, memory controller and core",
    "Verify every module with a cocotb harness rather than eyeballing waveforms",
    "Build axelcc, a C-subset compiler with a kernel-parameter ABI, so kernels are written in C rather than hand-assembled",
    "Take the design through a full open-source physical-design flow to a manufacturable, signoff-clean GDSII",
    "Prove the ISA on real workloads — transformer attention and a CNN keyword-spotting pipeline — running on the simulated RTL"
  ],
  "technologies": [
    {
      "name": "SystemVerilog",
      "description": "Primary RTL language for all 12 parameterized hardware modules. Parameterization allows DATA_MEM_ADDR_BITS, NUM_CORES, and THREADS_PER_BLOCK to be changed at elaboration time without touching any logic."
    },
    {
      "name": "Verilog",
      "description": "Used for FPGA-targeted flat synthesis file (gpu_combined.v) and compatibility with simulators that do not support full SystemVerilog."
    },
    {
      "name": "C",
      "description": "Implementation language for AXEL, the custom assembler library. Parses GPU assembly mnemonics and encodes them into the 4-format 32-bit binary instruction word."
    },
    {
      "name": "Python",
      "description": "Drives cocotb testbenches, orchestrates simulation runs, and implements the neural network training loop that generates kernel .hex files and checks convergence."
    },
    {
      "name": "cocotb",
      "description": "Python-based hardware co-simulation framework used for all unit and integration tests. Allows writing test logic in Python while running the actual RTL under Icarus Verilog."
    },
    {
      "name": "Icarus Verilog",
      "description": "Open-source Verilog/SystemVerilog simulator used as the cocotb backend. Compiles and elaborates the RTL, then hands control to cocotb coroutines for stimulus and checking."
    },
    {
      "name": "Q8 Fixed-Point Arithmetic",
      "description": "8-bit fractional encoding where real_value = stored_integer / 256. Enables neural network multiply-accumulate and gradient math on integer ALU hardware without any floating-point unit."
    },
    {
      "name": "AXEL Assembler",
      "description": "Custom C-library assembler that takes GPU assembly source (mnemonics + operands) and emits .hex kernel files. Handles all 4 instruction formats and the 21-instruction ISA encoding."
    },
    {
      "name": "GitHub Actions (CI)",
      "description": "Automated RTL test pipeline (.github/workflows/rtl-tests.yml) that runs the full cocotb suite on every push, keeping all 12 modules continuously verified."
    },
    {
      "name": "Makefile / Shell",
      "description": "Build automation for simulation (make sim), assembler compilation (make assembler), individual module tests (make test-<module>), and FPGA file generation."
    }
  ],
  "challenges": [
    {
      "title": "SIMD Thread Divergence with Activation Functions",
      "description": "ReLU's conditional branch (if x < 0, clamp to 0) causes thread divergence in SIMD execution — threads that take different branches must be serialised, collapsing parallelism and stalling the non-branching threads.",
      "solution": "Implemented branchless ReLU using bitwise masking on the sign bit of the Q8-encoded value. The mask is derived with a single SAR (arithmetic right shift by 31) that fills all bits with the sign, then ANDed with the input. All 16 threads execute identical instructions in lockstep — zero divergence, full throughput maintained through every activation layer."
    },
    {
      "title": "Signed Arithmetic for Gradient Descent in an Integer ISA",
      "description": "Gradient updates require signed multiply (weights × gradients) and arithmetic right shifts (to rescale Q8 products). A naive unsigned ISA silently produces wrong results for negative gradients, breaking convergence.",
      "solution": "Added two dedicated ISA instructions: IMUL (signed 32-bit multiply producing the lower 32 bits of the 64-bit product) and SAR (arithmetic right shift that preserves the sign bit). These two instructions make the full Q8 gradient-descent update expressible in native hardware without any software workaround."
    },
    {
      "title": "Per-Thread Hardware State Injection Without an OS",
      "description": "Each thread needs its own thread ID, block ID, and data slice pre-loaded into specific registers before the kernel executes — normally handled by a driver or OS. Without one, this state must come from the hardware itself.",
      "solution": "The Top-Level GPU RTL injects per-thread state (thread_id, block_id, data base address) directly into each core's register file at kernel launch, before the Scheduler begins the first pipeline cycle. Every thread gets its identity and partition with zero software overhead and no OS dependency."
    },
    {
      "title": "Seven-State Scheduler Correctness Across 4 Threads per Core",
      "description": "The Scheduler must multiplex 4 threads through the pipeline (IDLE → FETCH → WAIT → EXECUTE → UPDATE → DONE → WRITE_BACK) without starving any thread or corrupting in-flight state when a thread stalls on a memory access.",
      "solution": "Implemented a round-robin warp scheduler with per-thread state machines. Each thread tracks its own pipeline state independently; the Scheduler only advances a thread when its next stage is ready. Memory stalls park the affected thread in WAIT while others continue executing — maintaining throughput without a scoreboard."
    },
    {
      "title": "End-to-End Verification Across a 5-Layer Stack",
      "description": "Bugs can hide at any layer — ISA encoding, RTL logic, assembler codegen, kernel program, or the Python test driver — making root-cause analysis across a complete GPU stack extremely slow without layered isolation.",
      "solution": "Two-tier test strategy: cocotb unit tests for all 12 individual modules to isolate RTL bugs in isolation, then a single top-level integration test that runs the complete neural network training pipeline end-to-end. If unit tests pass but integration fails, the bug is in module interaction or the kernel program — not the RTL itself."
    },
    {
      "title": "FPGA Synthesis from Multi-File SystemVerilog",
      "description": "FPGA toolchains often struggle with multi-file SystemVerilog projects that use packages, interfaces, and parameterized generates — common in the modular RTL but problematic for Vivado/Quartus elaboration.",
      "solution": "Generated a flattened single-file gpu_combined.v using a shell script that concatenates all modules in dependency order, strips SystemVerilog-only constructs where Verilog-2001 equivalents exist, and adds XDC/SDC constraint stubs. The combined file synthesises cleanly on Xilinx and Lattice flows without any manual intervention."
    }
  ],
  "results": [
    "Signoff-clean GDSII on SkyWater 130 nm (Sky130A) through OpenLane 2.3.10 — 300,884 standard cells placed and routed",
    "Zero Magic DRC violations, and LVS clean at 188,812 devices",
    "Post-route static timing analysis closes at approximately 32.9 MHz in the typical corner, against a 40 MHz target",
    "391 cocotb tests across 15 RTL test suites, reported passing as of 2026-08-24",
    "28-instruction 32-bit ISA fully specified, with IMUL and SAR added for ML workload correctness",
    "axelcc compiles C-subset kernels to the GPU's ISA, with a kernel-parameter ABI so one binary serves many launch configurations",
    "Transformer multi-head attention runs on the GPU: the attention score, softmax and weighted-value kernels are address-parameterised, and a genuine two-head test with independent Q/K/V matches a Python reference",
    "Depthwise-separable CNN pipeline — stem convolution, depthwise, pointwise, maxpool, fully-connected and softmax kernels — with int8 fixed-point host tooling, verified end to end on the simulated RTL",
    "CALL/SRET subroutine support added to both the compiler and the RTL (call_stack.sv) to make the CNN kernels expressible",
    "A 4x4 linear + ReLU neural network trains end to end on the simulated hardware over 20 epochs, converging within 2.5% of the reference",
    "GitHub Actions CI runs the cocotb suite on every push, so regressions surface immediately"
  ],
  "futureWork": [
    "Tighten the floorplan to push utilisation below 27.9% and shrink the die — the run itself is estimated at around three days",
    "Resume the Tang Nano 20K FPGA port, currently paused in favour of compiler and kernel work",
    "Lift the current two-head ceiling on attention by moving off the flat memory layout, which today collides with the parameter base region at three heads",
    "Generalise axelcc from hand-written kernel sets toward a TFLite-to-axelcc backend",
    "Extend the ISA and register file toward wider warps and more concurrent cores"
  ],
  "repositories": [
    {
      "name": "32-bit-Tiny-GPU",
      "url": "https://github.com/austin207/32-bit-Tiny-GPU",
      "description": "Complete GPU stack: ISA spec, 12-module SystemVerilog RTL, AXEL assembler, 5 kernel demos, neural network training, cocotb tests, and FPGA port"
    }
  ],
  "gallery": [
    "/project-images/32-bit-tiny-gpu/GPU.png",
    "/project-images/32-bit-tiny-gpu/architecture.jpg",
    "/project-images/32-bit-tiny-gpu/instruction_encoding.jpg",
    "/project-images/32-bit-tiny-gpu/software_layer_architecture.jpg",
    "/project-images/32-bit-tiny-gpu/component-gpu.jpg",
    "/project-images/32-bit-tiny-gpu/component-core.jpg",
    "/project-images/32-bit-tiny-gpu/component-scheduler.jpg",
    "/project-images/32-bit-tiny-gpu/component-alu.jpg",
    "/project-images/32-bit-tiny-gpu/component-decoder.jpg",
    "/project-images/32-bit-tiny-gpu/component-fetcher.jpg",
    "/project-images/32-bit-tiny-gpu/component-dispatcher.jpg",
    "/project-images/32-bit-tiny-gpu/component-dcr.jpg",
    "/project-images/32-bit-tiny-gpu/component-lsu.jpg",
    "/project-images/32-bit-tiny-gpu/component-memory-controller.jpg",
    "/project-images/32-bit-tiny-gpu/component-pc.jpg",
    "/project-images/32-bit-tiny-gpu/component-register.jpg"
  ],
  "featured": true,
  "priority": 100,
  "documentationStatus": "complete"
}
